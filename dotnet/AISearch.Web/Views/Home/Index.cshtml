@{
    ViewData["Title"] = "AI Search - Multimodal Solution";
}

@* This view should not be used directly - the controller routes to AuthenticatedIndex or UnauthenticatedIndex *@
@* If you're seeing this, there may be a routing issue *@

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card material-card">
                <div class="card-body text-center">
                    <i class="material-icons text-warning" style="font-size: 48px;">warning</i>
                    <h4 class="mt-3">Routing Issue Detected</h4>
                    <p class="text-muted">This view should not be displayed directly. Please check the HomeController routing.</p>
                    <a asp-controller="Home" asp-action="Index" class="btn btn-primary">Refresh Page</a>
                </div>
            </div>
        </div>
    </div>
</div>